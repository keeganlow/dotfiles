# Borrowed & adapted from: https://github.com/brantb/dotfiles/blob/master/tmux.conf

# Set prefix to Ctrl-Space
unbind C-b

#if-shell 'test "$HHKB" = "true"' 'tmux set prefix C-a' 'set-option -g prefix F10'
set-option -g prefix F10

# Use vi keys
set -gw mode-keys vi

# don't require changing to normal mode to traverse command history
bind-key -t vi-edit Up   history-up
bind-key -t vi-edit Down history-down
set-option -g history-limit 10000

# hey tmux! use the system clipboard.
# this will work if you're using mvim from within the terminal
set-option -g default-command "reattach-to-user-namespace -l bash"

# so you don't end up with window numbers like: 0 1 4 5 6
set-option -g renumber-windows on

# Mouse support
set -g mouse-select-pane on
set -g mouse-resize-pane on
set -g mouse-select-window on
setw -g mode-mouse copy-mode

# 256 color support
set -g default-terminal "screen-256color"

# Start window numbers from 1
set -g base-index 1

# set escape time so vim is responsive inside tmux
set -g escape-time 1

# prefix r: reload tmux config
bind r source-file ~/.tmux.conf \; display "Reloaded ~/.tmux.conf"

# make new panes and windows use the pwd
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

# remap pane movement commands
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# remap pane resizing commands
#bind -r H resize-pane -L 5
#bind -r J resize-pane -D 5
#bind -r K resize-pane -U 5
#bind -r L resize-pane -R 5

# smart pane switching with awareness of vim splits
bind -n C-h run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys C-h) || tmux select-pane -L"
bind -n C-j run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys C-j) || tmux select-pane -D"
bind -n C-k run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys C-k) || tmux select-pane -U"
bind -n C-l run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys C-l) || tmux select-pane -R"
bind -n C-\ run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys 'C-\\') || tmux select-pane -l"

bind C-l send-keys 'C-l'

bind-key [ copy-mode
unbind-key C-[
bind-key ] paste-buffer

#Setup 'v' to begin selection as in Vim
bind-key -t vi-copy v begin-selection
bind-key -t vi-copy y copy-pipe "reattach-to-user-namespace pbcopy"

# Update default binding of `Enter` to also use copy-pipe
unbind -t vi-copy Enter
bind-key -t vi-copy Enter copy-pipe "reattach-to-user-namespace pbcopy"

# instant begin-selection - "nice"
# bind -n C-[ run "tmux copy-mode && tmux send-keys -l v"

# Status Bar
set-option -g status-interval 1
#set-option -g status-left ''
#set-option -g status-right '%l:%M%p'
#set-window-option -g window-status-current-fg magenta
#set-option -g status-fg default
##
## # Status Bar solarized-dark (default)
#set-option -g status-bg black
#set-option -g pane-active-border-fg
#set-option -g pane-border-fg black

#### COLOUR (Solarized 256)
#
## default statusbar colors
set-option -g status-bg colour235 #base02
set-option -g status-fg colour136 #yellow
set-option -g status-attr default
#
## default window title colors
set-window-option -g window-status-fg colour244 #base0
set-window-option -g window-status-bg default
##set-window-option -g window-status-attr dim
#
## active window title colors
set-window-option -g window-status-current-fg colour50 # lightblue
set-window-option -g window-status-current-bg default
##set-window-option -g window-status-current-attr bright
#
## pane border
set-option -g pane-border-fg colour235 #base02
set-option -g pane-active-border-fg colour240 #base01
#
## message text
set-option -g message-bg colour235 #base02
set-option -g message-fg colour166 #orange
#
## pane number display
set-option -g display-panes-active-colour colour33 #blue
set-option -g display-panes-colour colour166 #orange
#
## clock
set-window-option -g clock-mode-colour colour43 #green

set -g status-utf8 on

# visual notification of activity in other windows
setw -g monitor-activity on
set -g visual-activity on
# indicate whether Prefix has been caputred + time in the right-status area
# full path needed because spotifysong is not in the PATH for non-interactive shells (how tmux runs this file)
set -g status-right '#{?client_prefix,#[reverse]<Prefix>#[noreverse] ,} %H:%M ♫  #(~/scripts/bash/spotifysong) ♫ '

# https://github.com/mattly/iterm-colors-pencil/blob/master/tmux/tmux-pencilcolors.conf
# statusline
# set -g status-bg default # transparent
# set -g status-fg magenta
# set -g status-attr default
# 
# # default window title colors
# setw -g window-status-fg blue
# setw -g window-status-bg default
# setw -g window-status-attr dim
# 
# # active window title colors
# setw -g window-status-current-fg brightred
# setw -g window-status-current-bg default
# setw -g window-status-current-attr bright
# 
# # bell alerted window color
# setw -g window-status-bell-bg red
# setw -g window-status-bell-fg white
# setw -g window-status-bell-attr bright
# 
# # content alerted color
# setw -g window-status-content-bg blue
# setw -g window-status-content-fg white
# setw -g window-status-content-attr bright
# setw -g window-status-activity-bg blue
# setw -g window-status-activity-fg white
# setw -g window-status-activity-attr bright
# 
# # pane borders
# set -g pane-border-fg white
# set -g pane-border-bg default
# set -g pane-active-border-fg brightblack
# set -g pane-active-border-bg default
# 
# # command line
# set -g message-fg default
# set -g message-bg default
# set -g message-attr bright

# List of plugins
# Supports `github_username/repo` or full git repo URLs
set -g @tpm_plugins '              \
  tmux-plugins/tpm                 \
  tmux-plugins/tmux-open           \
  tmux-plugins/tmux-sessionist     \
  tmux-plugins/tmux-yank           \
'

# Other examples:
# github_username/plugin_name    \
# git@github.com/user/plugin     \
# git@bitbucket.com/user/plugin  \

# Initializes TMUX plugin manager.
# Keep this line at the very bottom of tmux.conf.
run-shell '~/.tmux/plugins/tpm/tpm'
